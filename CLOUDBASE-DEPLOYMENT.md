# CloudBase CLI 部署指南

## 📋 前提条件

1. **安装 Node.js**：确保已安装 Node.js 环境（建议版本 14.0+）
2. **安装 CloudBase CLI**：
   ```bash
   npm install -g @cloudbase/cli
   ```
3. **拥有腾讯云账号**：并已开通云开发环境

## 🚀 部署步骤

### 1. 登录 CloudBase CLI

执行以下命令，在浏览器中完成授权登录：

```bash
cloudbase login
```

或使用新命令：

```bash
tcb login
```

### 2. 选择云开发环境

查看可用的云开发环境：

```bash
cloudbase env list
```

或使用新命令：

```bash
tcb env list
```

### 3. 配置环境 ID

编辑 `cloudbaserc.json` 文件，将 `envId` 字段替换为你的云开发环境 ID：

```json
{
  "envId": "你的云开发环境ID",
  "version": "2.0",
  "framework": {
    "name": "static-website",
    "plugins": {
      "hosting": {
        "use": "@cloudbase/framework-plugin-hosting",
        "inputs": {
          "outputPath": ".",
          "cloudPath": "/"
        }
      }
    }
  }
}
```

### 4. 部署项目

执行以下命令部署项目：

```bash
cloudbase framework deploy
```

或使用新命令：

```bash
tcb framework deploy
```

### 5. 访问网站

部署完成后，你可以通过以下方式访问网站：

1. **默认域名**：`https://你的环境ID.tcloudbaseapp.com`
2. **自定义域名**：如果已配置自定义域名，可以通过自定义域名访问

## ⚙️ 配置说明

### cloudbaserc.json 配置项

| 配置项 | 说明 |
|-------|------|
| envId | 云开发环境 ID |
| version | 配置文件版本 |
| framework.name | 框架名称 |
| framework.plugins.hosting.use | 使用的托管插件 |
| framework.plugins.hosting.inputs.outputPath | 本地静态文件目录 |
| framework.plugins.hosting.inputs.cloudPath | 云端存储路径 |

### 静态网站托管配置

部署完成后，你可以在腾讯云控制台的云开发环境中：

1. 查看静态网站托管状态
2. 配置自定义域名
3. 查看访问统计
4. 管理静态文件

## 📁 项目结构说明

本项目是一个纯静态网站，包含以下主要文件：

- `index.html` - 主页面
- `styles.css` - 样式文件
- `script.js` - 交互脚本
- `styles-contact-form-fixed.css` - 联系表单样式
- `script-fixed.js` - 修复后的脚本
- `cloudbaserc.json` - CloudBase 部署配置

## 🔧 常见问题

### 1. 部署失败

**问题**：部署时提示 "权限不足"
**解决方案**：确保登录的腾讯云账号有该云开发环境的管理权限

### 2. 网站访问 404

**问题**：部署后访问网站出现 404 错误
**解决方案**：
- 检查 `index.html` 是否存在于根目录
- 检查 `cloudbaserc.json` 中的 `outputPath` 配置是否正确
- 检查云开发环境是否已开启静态网站托管功能

### 3. 资源加载失败

**问题**：网站加载时部分资源（如图片、样式）无法加载
**解决方案**：
- 检查资源文件路径是否正确
- 确保资源文件已正确上传到云开发存储

## 📞 技术支持

- **腾讯云文档**：[云开发静态网站托管](https://cloud.tencent.com/document/product/876/32996)
- **CloudBase CLI 文档**：[CloudBase CLI 命令参考](https://cloud.tencent.com/document/product/876/41391)

## 🚀 快速部署命令汇总

```bash
# 安装 CloudBase CLI
npm install -g @cloudbase/cli

# 登录
cloudbase login

# 查看环境
cloudbase env list

# 编辑 cloudbaserc.json，配置 envId

# 部署
cloudbase framework deploy
```

---

**© 2025 流云智炬科技. 版权所有**